spring:
  datasource:
    url: jdbc:mysql://localhost:3306/restaurant_db
    username: root
    password: Arman@1981

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true