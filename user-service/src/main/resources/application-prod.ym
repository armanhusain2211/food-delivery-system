server:
  port: 8081

spring:
  application:
    name: user-service

  datasource:
    url: jdbc:mysql::3306/user_db
    username: root
    password: Arman@1981

  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: false

  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver