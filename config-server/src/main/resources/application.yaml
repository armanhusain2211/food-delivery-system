server:
  port: 8888

spring:
  application:
    name: config-server

  security:
    user:
      name: configuser
      password: configpass

  cloud:
    config:
      server:
        git:
          uri: https://github.com/armanhusain2211/food-config-repo.git
          default-label: main
          clone-on-start: true